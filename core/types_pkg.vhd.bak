library IEEE;
use IEEE.STD_LOGIC_1164.all;

package lsu_pkg is
    type statetype is (IDLE, REQUESTING, WAITING, DONE);
    -- Array of 4 statetype values for 4 LSUs
    type statetype_array is array (3 downto 0) of statetype;
end package;

package body lsu_pkg is
end package body;